# Shared config for EchoTwin. Copy to .env and fill in real values.
# Current shared Tailscale test network.
# A physical ESP32 cannot route to these Tailscale addresses directly; when
# testing the real board, use the backend Mac's Wi-Fi/hotspot LAN address.

SERVER_IP=100.93.145.13
FRONTEND_IP=100.117.169.11
FIRMWARE_LAPTOP_IP=100.105.226.40

# Mosquitto runs on the backend Mac. This address works for laptop-based tests.
MQTT_BROKER_HOST=100.93.145.13
MQTT_BROKER_PORT=1883

# Reject broken-clock sensor readings outside this live-data window
SENSOR_TIMESTAMP_MAX_AGE_SECONDS=300
SENSOR_TIMESTAMP_FUTURE_TOLERANCE_SECONDS=60

# PostgreSQL / TimescaleDB
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=echotwin
POSTGRES_USER=echotwin
POSTGRES_PASSWORD=changeme

# Browser origins allowed to call the REST API (comma-separated)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5173,http://127.0.0.1:5173,http://100.117.169.11:3000,http://100.117.169.11:5173

# Leave blank because Ollama is not used by this backend.
TAILSCALE_OLLAMA_URL=
OLLAMA_MODEL=llama3.1:8b
OLLAMA_TIMEOUT_SECONDS=2.5
