# Points the dashboard at the mock WS sender by default so frontend work
# can proceed without the real backend running.
# Swap to the real backend later, e.g. ws://<SERVER_IP>:8000/ws/room-status
VITE_WS_URL=ws://localhost:8001
