# Copy to .env.local and adjust for your network. Read server-side only,
# inside the /api/state and /api/ask route handlers.

# Aditya's backend HTTP server. Placeholder until confirmed.
BACKEND_URL=http://192.168.1.100:8000

# Local LLM (Ollama), reachable only over Tailscale.
OLLAMA_URL=http://100.69.187.59:11434
OLLAMA_MODEL=qwen3:8b
