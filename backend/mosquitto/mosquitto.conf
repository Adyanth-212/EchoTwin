# Minimal Mosquitto config for local hackathon development.
# No auth, no TLS — this is a closed venue LAN (phone hotspot), not
# internet-facing. Do not reuse this config outside that context.

listener 1883
allow_anonymous true
